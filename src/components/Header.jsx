import Version from "./Version.jsx";

const Header = () => {
    return (
        <header>
            <div className="logo">PIZZA DAY</div>
            <Version />
        </header>
    );
};

export default Header;
